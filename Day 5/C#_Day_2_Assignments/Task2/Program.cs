namespace Task2
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Design a C# program with a method that attempts to access a local variable outside its scope, a constant, and a static variable.
            //Document the results in comments within the code explaining why each access either succeeds or fails.
            /*        void test()
                    {
                        int num = 10;
                    }
                    void Test2()
                    {
                        Console.WriteLine(num); // Cannot access the local variable num in this scope
                        static int[] arr = { 1, 2, 3, 4, 5 };
                        const int number = 1;

                    }*/
        }
    }
}
